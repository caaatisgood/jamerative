{
  "name": "@collab/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "subgraph:auth": "yarn workspace @collab/subgraph auth",
    "subgraph:codegen": "yarn workspace @collab/subgraph codegen",
    "subgraph:build": "yarn workspace @collab/subgraph build",
    "subgraph:deploy": "yarn workspace @collab/subgraph deploy",
    "react-app:build": "yarn workspace @collab/react-app build",
    "react-app:eject": "yarn workspace @collab/react-app eject",
    "react-app:ipfs": "yarn workspace @collab/react-app ipfs",
    "react-app:start": "yarn workspace @collab/react-app start",
    "react-app:test": "yarn workspace @collab/react-app test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
